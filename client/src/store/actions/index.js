import { loginUser, createUser, logoutUser, authenticate } from './login'
import { createPost, getPosts } from './posts'

export default {
  loginUser,
  logoutUser,
  createPost,
  authenticate,
  createUser,
  getPosts
}
