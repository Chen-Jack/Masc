import { loginUser, createUser, logoutUser, authenticate } from './login'
import { createPost } from './posts'

export default {
  loginUser,
  logoutUser,
  createPost,
  authenticate,
  createUser
}
